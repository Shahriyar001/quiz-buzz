import React from 'react';

const Option = ({ option }) => {
    console.log(option)
    // const option = option;
    return (
        <div>
            <h5>{option}</h5>
        </div>
    );
};

export default Option;